## p041

分布式数据库

[图] 分布式数据库体系结构图（自上而下分层）：

- 顶层：三个并列方框「全局外模式」「全局外模式」「全局外模式」
- 虚线，右侧标注「映像1」
- 「全局概念模式」方框
- 虚线，右侧标注「映像2」
- 「分片模式」方框
- 虚线，右侧标注「映像3」
- 「分布模式」方框
- 虚线，右侧标注「映像4」
- 三个并列方框「局部概念模式」「局部概念模式」「局部概念模式」
- 每个「局部概念模式」向下连接一个「局部内模式」方框，每个「局部内模式」向下连接一个「局部数据库」圆柱体
- 左侧六边形「全局DBMS」，以红线分别连接全局概念模式、分片模式、分布模式（及全局外模式层）；左侧圆形「局部DBMS」连接局部概念模式与局部内模式
- 右侧标注：上半部分（全局外模式至分布模式，即映像4虚线以上）为红色双向箭头，标注「分布式数据库（增加的）」；下半部分（局部概念模式至局部数据库）为黑色双向箭头，标注「集中式数据库（原有的）」

## p042

分布式数据库

➤ 分布透明性

- 分片透明性：分不分片，用户感受不到（不关心如何分片存储）。
- 位置透明性：数据存放在哪里，用户不用管（用户无需知道数据存放的物理位置）。
- 复制透明：不关心结点的复制情况
- 局部数据模型透明性（逻辑透明）：用户或应用程序无需知道局部场地使用的是哪种数据模型。

[方框]（由「分片透明性」处箭头指向）：

- 水平分片：按记录分
- 垂直分片：按字段分
- 混合分片

[图] 数据表分片示意图：「数据表」网格；右侧按行划分为「第1片」「第2片」两组，括号标注「水平分片」；底部按列划分为「第1片」「第2片」两组，括号标注「垂直分片」。

## p043

分布式数据库

分布式事务管理

两阶段提交协议 2PC

➤ 2PC事务提交的两个阶段

表决阶段，目的是形成一个共同的决定。

执行阶段，目的是实现这个协调者的决定。

➤ 两条全局提交规则

只要有一个参与者撤销事务，协调者就必须做出全局撤销决定。

只有所有参与者都同意提交事务，协调者才能做出全局提交决定。

## p044

分布式数据库

分布式事务管理

[图] 两阶段提交协议活动图（左列：协调者；右列：参与者）：

协调者侧流程：

- 初始 → 写begin_commit到日志 → 等待 → 有要求撤销？
- 有要求撤销？—Yes→ 写abort到日志 → 撤销 → 写end_trans到日志
- 有要求撤销？—No→ 写commit到日志 → 提交 → 写end_trans到日志

参与者侧流程：

- 初始 → 准备提交？
- 准备提交？—No→ 写abort到日志
- 准备提交？—Yes→ 写ready到日志 → 就绪 → 消息类型？
- 消息类型？—撤销→ 写abort到日志 → 撤销
- 消息类型？—提交→ 写commit到日志 → 提交

协调者与参与者之间的虚线消息箭头：

- 协调者「写begin_commit到日志」→ 参与者「初始」：准备提交
- 参与者「写abort到日志」（No分支）→ 协调者「等待」：建议撤销
- 参与者「写ready到日志」→ 协调者「等待」：建议提交
- 协调者「写abort到日志」（Yes分支）→ 参与者「就绪」：全局撤销
- 协调者「写commit到日志」→ 参与者「就绪」：全局提交
- 参与者「写abort到日志」（撤销分支）→ 协调者「撤销」：确认
- 参与者「写commit到日志」（提交分支）→ 协调者「提交」：确认

图下方标题：两阶段提交协议活动图

## p045

分布式数据库

➤ 分布式数据库管理系统—组成

- LDBMS
- GDBMS
- 全局数据字典
- 通信管理（CM）

➤ 分布式数据库管理系统—结构

- 全局控制集中的DDBMS
- 全局控制分散的DDBMS
- 全局控制部分分散的DDBMS

## p046

NoSQL

✓ NoSQL（Not-only SQL）：不仅仅只是SQL，泛指非关系型的数据库。

| 对比维度 | 关系数据库 | NoSQL |
| --- | --- | --- |
| 应用领域 | 面向通用领域 | 特定应用领域 |
| 数据容量 | 有限数据 | 海量数据 |
| 数据类型 | 结构化数据【二维表】 | 非结构化数据 |
| 并发支持 | 支持并发、但性能低 | 高并发 |
| 事务支持 | 高事务性 | 弱事务性 |
| 扩展方式 | 向上扩展 | 向外扩展 |

## p047

NoSQL

| 分类 | 典型应用场景 | 数据模型 | 优点 | 缺点 | 举例 |
| --- | --- | --- | --- | --- | --- |
| 键值（key-value） | 内容缓存，主要用于处理大量数据的高访问负载，也用于一些日志系统等等。 | Key 指向 Value 的键值对，通常用hash table来实现 | 查找速度快 | 数据无结构化，通常只被当作字符串或者二进制数据 | Redis, Tokyo Cabinet/Tyrant, Voldemort, Oracle BDB |
| 列存储数据库 | 分布式的文件系统 | 以列簇式存储，将同一列数据存在一起 | 查找速度快，可扩展性强，更容易进行分布式扩展 | 功能相对局限 | HBase, Cassandra, Riak |

## p048

NoSQL

| 分类 | 典型应用场景 | 数据模型 | 优点 | 缺点 | 举例 |
| --- | --- | --- | --- | --- | --- |
| 文档型数据库 | Web应用（与Key-Value类似，Value是结构化的，不同的是数据库能够了解Value的内容） | Key-Value对应的键值对，Value为结构化数据 | 数据结构要求不严格，表结构可变，不需要像关系型数据库一样需要预先定义表结构 | 查询性能不高，而且缺乏统一的查询语法。 | CouchDB, MongoDb |
| 图形数据库（Graph） | 社交网络，推荐系统等。专注于构建关系图谱 | 图结构 | 利用图结构相关算法。比如最短路径寻址，N度关系查找等 | 很多时候需要对整个图做计算才能得出需要的信息，而且这种结构不太好做分布式的集群方案。 | Neo4J, InfoGrid, Infinite Graph |
