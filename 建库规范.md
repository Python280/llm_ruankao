# llm_ruankao · 软考架构师学习库

软考高级·系统架构设计师多源融合学习库。`StudyVault/` = Obsidian 笔记库；`artifacts/` = 源素材产物；`scripts/` = 管线脚本；`第N章：XX/` = 讲义原件。

## 建章管线

建新章节用项目级 skill **ruankao-build**（触发："建第X章"）。约定以本文件为准。

## 源角色与页码约定

| 书 | 简称 | 角色 | 页码换算 |
|---|---|---|---|
| 希赛讲义 PPT（各章 PDF） | 讲义 | 骨架（结构/星级/考情/真题），无文字层→视觉读 | 物理页=渲染 pNNN |
| 一本通·精华知识点 | 一本通 | 补浓缩图表/口诀 | 印刷=物理 |
| 32小时通关 | 32h | 补题 | 物理=印刷+9 |
| 官方教程[第二版] | 官方 | 按需字典（补定义/仲裁冲突） | 印刷=物理−15 |

冲突规则：考情跟讲义；定义跟官方；真冲突 [!warning] 两说并存（考试跟讲义）。讲义独有标"无旁证，考试跟讲义"。

## 硬约定

- 主题文件夹四位编号跟讲义主节（01xx/02xx/03xx…）；concepts/ 追踪表同号命名
- 补肉必标来源：frontmatter `supplements: [一本通 pNN, …]` + 节末 `> [!tip] 来源：书简称 pNN`
- 练习题 ≥8 题/夹；题源优先 讲义真题 > 32h > 原文逐字改判断题；〔回忆〕≥60%；[!answer]- 折叠
- 产物 JSON 用中文键（子节点/内容/类型/层级/页）
- 骨架划分必须给用户过目再动笔；dashboard 统计数字由用户自己维护
- 全程中文回复；文件夹名禁用 `/`（TCP/IP→TCP_IP），wikilink 按实际文件名

## 视觉读图坑（重要）

- 一次 Read 多张大图撞 6MB 请求体上限，会话报废 → fitz 渲染 JPEG（q75）+ subagent 分批读图（≤8 页/agent），主上下文只收文本
- subagent 用 Write 直接落盘抽取结果，不回传内容
- 完成的 subagent 不要 SendMessage resume（图上文 URL 失效报 400）；需要时从磁盘 transcript 提取
- glm-5.2 无视觉；qwen3.8-max 读图正常

## 管线脚本（.claude/skills/ruankao-build/scripts/，CWD 须为项目根）

- build_toc_index.py — 官方书书签→toc_index.json（一次性，已跑）
- locate_section.py — 官方书定位/摘录（--out 纯文件名落 artifacts/；带目录相对路径基于 CWD）
- render_pages.py — 扫描书/讲义渲染 PNG
- check_links.py — 断链/题数/frontmatter 质检，交付前必须零缺陷

artifacts/ 永久产物：toc_index.json、scan_toc_*.json（扫描书目录缓存）、chNN/ 的讲义 JSON+mapping+excerpts+official_*.md；渲染中间产物与 part_*.json 抽完即删。大纲全文转录在 artifacts/dagang/dagang_full.md，一次解析、之后复用：建后续章直接读它比对考点，不重复转录；交付时比对一次。
